You are helping with a React + TypeScript merchant dashboard.
Use TanStack Query for data fetching.
Use Tailwind CSS for styling — no inline styles.
The API is at /api/ and returns { data: ... } or { error: { code, message } }.
