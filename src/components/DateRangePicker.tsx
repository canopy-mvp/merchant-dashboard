import React from 'react';

export function DateRangePicker({ onChange }: { onChange: (start: Date, end: Date) => void }) {
  return <div className="flex gap-2">Date range picker placeholder</div>;
}
